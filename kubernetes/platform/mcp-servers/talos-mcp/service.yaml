apiVersion: v1
kind: Service
metadata:
  name: talos-mcp
  namespace: mcp-servers
  labels:
    app: talos-mcp
spec:
  type: NodePort
  selector:
    app: talos-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31086
    protocol: TCP
    name: http
