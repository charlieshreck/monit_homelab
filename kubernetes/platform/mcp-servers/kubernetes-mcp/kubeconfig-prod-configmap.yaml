apiVersion: v1
kind: ConfigMap
metadata:
  name: kubeconfig-prod
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: kubernetes-prod-mcp
    cluster: production-talos
data:
  kubeconfig: |
    # IMPORTANT: Paste the contents of /home/prod_homelab/infrastructure/terraform/generated/kubeconfig here
    # This file is gitignored and must be manually created during deployment
    # Run: kubectl create configmap kubeconfig-prod --from-file=kubeconfig=/home/prod_homelab/infrastructure/terraform/generated/kubeconfig -n mcp-servers
