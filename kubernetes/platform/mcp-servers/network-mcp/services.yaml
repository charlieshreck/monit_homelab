apiVersion: v1
kind: Service
metadata:
  name: opnsense-mcp
  namespace: mcp-servers
  labels:
    app: opnsense-mcp
spec:
  type: NodePort
  selector:
    app: opnsense-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31087
    protocol: TCP
    name: http
---
apiVersion: v1
kind: Service
metadata:
  name: unifi-mcp
  namespace: mcp-servers
  labels:
    app: unifi-mcp
spec:
  type: NodePort
  selector:
    app: unifi-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31088
    protocol: TCP
    name: http
---
apiVersion: v1
kind: Service
metadata:
  name: adguard-mcp
  namespace: mcp-servers
  labels:
    app: adguard-mcp
spec:
  type: NodePort
  selector:
    app: adguard-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31089
    protocol: TCP
    name: http
