---
# OPNsense credentials (use existing infrastructure path)
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: mcp-opnsense
  namespace: mcp-servers
  labels:
    app: opnsense-mcp
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /infrastructure/opnsense
  managedSecretReference:
    secretName: mcp-opnsense
    secretNamespace: mcp-servers
    secretType: Opaque
    creationPolicy: Owner
---
# AdGuard credentials (use existing infrastructure path)
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: mcp-adguard
  namespace: mcp-servers
  labels:
    app: adguard-mcp
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /infrastructure/adguard
  managedSecretReference:
    secretName: mcp-adguard
    secretNamespace: mcp-servers
    secretType: Opaque
    creationPolicy: Owner
---
# UniFi credentials (use existing apps path)
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: mcp-unifi
  namespace: mcp-servers
  labels:
    app: unifi-mcp
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /apps/unifi
  managedSecretReference:
    secretName: mcp-unifi
    secretNamespace: mcp-servers
    secretType: Opaque
    creationPolicy: Owner
