apiVersion: v1
kind: Service
metadata:
  name: infisical-mcp
  namespace: mcp-servers
  labels:
    app: infisical-mcp
spec:
  type: NodePort
  selector:
    app: infisical-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31080
    protocol: TCP
    name: http
