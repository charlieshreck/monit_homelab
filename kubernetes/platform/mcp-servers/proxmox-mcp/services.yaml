apiVersion: v1
kind: Service
metadata:
  name: proxmox-ruapehu-mcp
  namespace: mcp-servers
  labels:
    app: proxmox-ruapehu-mcp
spec:
  type: NodePort
  selector:
    app: proxmox-ruapehu-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30082
    protocol: TCP
    name: http
---
apiVersion: v1
kind: Service
metadata:
  name: proxmox-carrick-mcp
  namespace: mcp-servers
  labels:
    app: proxmox-carrick-mcp
spec:
  type: NodePort
  selector:
    app: proxmox-carrick-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30083
    protocol: TCP
    name: http
