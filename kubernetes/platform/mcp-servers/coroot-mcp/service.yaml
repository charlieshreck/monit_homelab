apiVersion: v1
kind: Service
metadata:
  name: coroot-mcp
  namespace: mcp-servers
  labels:
    app: coroot-mcp
spec:
  type: NodePort
  selector:
    app: coroot-mcp
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31081
    protocol: TCP
    name: http
